machine:
   services:
      - docker

dependencies:
   override:
      - npm install
      - docker build -t instanews .

test:
   override:
      - docker run -t -d -p 3000:3000 -name insta instanews
      - npm test 

deployment:
  hub:
    branch: master
    commands:
      - $DEPLOY
