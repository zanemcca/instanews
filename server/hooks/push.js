module.exports = function(app) {

   /*
   var Push = app.models.push;

   console.log('Setting up Push hooks!');

   Push.beforeRemote( '**', function(ctx, push, next) {
      console.log('Before notify!');
      next();
   });

   Push.afterRemote( '**', function(ctx, push, next) {
      console.log('After notify!');
      next();
   });
   */

};
